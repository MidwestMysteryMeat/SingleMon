-- config.lua
-- Optional: point assetPath at a folder of creature/tile sprites to render them
-- instead of colored shapes. The engine runs fine without it (see README).
-- Sprites are NOT bundled with this repo.
return {
  assetPath = "assets",   -- e.g. an absolute path to your sprite folder
  tileSize  = 32,
  scale     = 2,
}
